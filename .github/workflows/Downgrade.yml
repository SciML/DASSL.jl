name: Downgrade
on:
  pull_request:
    branches:
      - master
    paths-ignore:
      - 'docs/**'
  push:
    branches:
      - master
    paths-ignore:
      - 'docs/**'
jobs:
  test:
    uses: "SciML/.github/.github/workflows/downgrade.yml@v1"
    with:
      allow-reresolve: true
      julia-version: "1.10"
      skip: "Pkg,TOML"
    secrets: "inherit"
